{
  "name": "salvageunion-reference",
  "version": "2.3.2",
  "description": "Comprehensive, schema-validated JSON dataset and TypeScript ORM for the Salvage Union tabletop RPG",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "development": "./lib/index.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./data/*": {
      "types": "./data/*.d.ts",
      "import": "./data/*.json",
      "default": "./data/*.json"
    },
    "./schemas/*": {
      "types": "./schemas/*.d.ts",
      "import": "./schemas/*.json",
      "default": "./schemas/*.json"
    },
    "./package.json": "./package.json"
  },
  "sideEffects": false,
  "files": [
    "dist",
    "lib",
    "data",
    "schemas",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "rm -rf dist && bun test && bun run docs && tsc --project tsconfig.build.json && bun run validate:all && bun run format && bun run lint",
    "build:quick": "tsc --project tsconfig.build.json",
    "build:incremental": "tsc --project tsconfig.build.json --incremental",
    "watch": "tsc --project tsconfig.build.json --watch",
    "watch:quick": "tsc --project tsconfig.build.json --watch",
    "prepare": "tsc --project tsconfig.build.json",
    "prepublishOnly": "bun run build && bun run typecheck",
    "validate:ids": "tsx tools/checkUniqueIds.ts",
    "fix:ids": "tsx tools/generateMissingIds.ts",
    "docs": "tsx tools/generateDocs.ts",
    "docs:schemas": "tsx tools/generateSchemaDocs.ts",
    "generate:enums": "tsx tools/generateEnumTypes.ts",
    "generate:common": "tsx tools/generateCommonTypes.ts",
    "generate:objects": "tsx tools/generateObjectTypes.ts",
    "generate:schemas": "tsx tools/generateSchemaTypes.ts",
    "generate:utilities": "tsx tools/generateUtilities.ts",
    "generate:typeIndex": "tsx tools/generateTypeIndex.ts",
    "generate:types": "bun run generate:enums && bun run generate:common && bun run generate:objects && bun run generate:schemas",
    "generate:index": "tsx tools/generateIndexFile.ts",
    "generate": "bun run generate:types && bun run generate:typeIndex && bun run generate:index && bun run generate:utilities && bun run lint -- --fix && bun run format",
    "validate:references": "tsx tools/validateReferences.ts",
    "validate:actions": "tsx tools/validateActionReferences.ts",
    "validate:all": "bun run validate:ids && bun run validate:references && bun run validate:actions",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "lint": "eslint .",
    "format": "prettier --write .",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/alxjrvs/salvageunion-data.git"
  },
  "keywords": [
    "salvage-union",
    "tabletop-rpg",
    "ttrpg",
    "game-data",
    "json-schema",
    "leyline-press",
    "orm",
    "typescript",
    "mech",
    "post-apocalyptic",
    "reference-data",
    "game-mechanics",
    "schema-validation",
    "type-safe"
  ],
  "author": "Alex Jarvis",
  "license": "Salvage Union Open Game Licence 1.0b",
  "bugs": {
    "url": "https://github.com/alxjrvs/salvageunion-data/issues"
  },
  "homepage": "https://github.com/alxjrvs/salvageunion-data#readme",
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/bun": "^1.3.2",
    "@types/node": "^24.10.1",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "bun-types": "^1.3.2",
    "eslint": "^9.39.1",
    "json-schema-to-typescript": "^15.0.4",
    "jsonschema": "^1.5.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.47.0",
    "vitest": "^4.0.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
